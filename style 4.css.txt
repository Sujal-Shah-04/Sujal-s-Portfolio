body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: linear-gradient(120deg, #0a0f3d, #f5e1c8);
    color: white;
    text-align: center;
}

nav {
    display: flex;
    justify-content: center;
    background: rgba(0, 0, 0, 0.8);
    padding: 15px;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 20px;
}

nav ul li a {
    color: white;
    text-decoration: none;
    font-size: 18px;
    transition: color 0.3s;
}

nav ul li a:hover {
    color: #f39c12;
}

section {
    padding: 50px;
}

.home-content {
    position: relative;
    text-align: center;
    padding: 100px 20px;
}

.home-content h1 {
    font-size: 50px;
    animation: fadeIn 2s ease-in, spotlight 3s infinite alternate;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes spotlight {
    from {
        text-shadow: 0 0 10px rgba(255, 255, 255, 0.8), 0 0 20px rgba(255, 255, 255, 0.6);
    }
    to {
        text-shadow: 0 0 20px rgba(255, 255, 255, 1), 0 0 30px rgba(255, 255, 255, 0.8);
    }
}

.about-container {
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: left;
    max-width: 900px;
    margin: auto;
    gap: 30px;
}

.about-container img {
    width: 200px;
    height: 200px;
    border-radius: 50%;
    object-fit: cover;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

.about-text {
    max-width: 500px;
}

.skill-container {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
}

.skill-box {
    background: #444;
    border-radius: 50%;
    padding: 20px;
    width: 150px;
    height: 150px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    transition: transform 0.3s;
}

.skill-box:hover {
    transform: scale(1.1);
    background: #f39c12;
}

.skill-bar {
    background: #444;
    border-radius: 10px;
    padding: 5px;
    margin: 10px auto;
    width: 80%;
}

.progress {
    height: 10px;
    background: #f39c12;
    border-radius: 10px;
}

.skill-btn {
    margin-top: 10px;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    background: #f39c12;
    color: white;
    cursor: pointer;
    transition: background 0.3s;
}

.skill-btn:hover {
    background: #e67e22;
}

form input, form textarea {
    width: 80%;
    padding: 10px;
    margin: 10px 0;
    border: none;
    border-radius: 5px;
}

form button {
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    background: #f39c12;
    color: white;
    cursor: pointer;
    transition: background 0.3s;
}

form button:hover {
    background: #e67e22;
}

footer {
    text-align: center;
    padding: 20px;
    background: rgba(0, 0, 0, 0.8);
}